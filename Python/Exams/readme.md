 Тестирование на Питоне, все задания отдельно по файлам исходных кодов и файлам вывода

 Напоминание: двумерные списки и установка значений, пример.

    Краткое описание или примерные задания:

01  Решение на условных графах по рисунку, без кода.

02	Логические выражения и операторы.

04	Кодирование и декодирование информации префиксным кодом.

05	Двоичное кодирование и алгоритмы.

06  Задание на черепашку, решается не кодом.

07  Таблицу построить вместе.

08  Системы счисления и комбинации цифр.

09  Решаем кодом! Посмотреть самому, Задние 9, номера: 40725, 48430, 55626, 68510, 64894.

10  Пропускаем, офисный пакет.

11  По аналогии с 7-ым, только устно, номера по выбору.

12  Кодом. Задание №12, варианты:  11310, 11350, 15854.

13  Кодом с библиотекой, на адреса возможно пропустить или выбрать другой вариант.

14


print('\n    Задание 47216. Также может встречаться, немного необычно по формулировке, но последовательность в '
      'любом случае должна сходиться. При случайных позициях в цифрах, всё равно результат будет. Лучше сразу '
      'посмотреть внимательно на правила, и определить сходимость. Ответ 749.')
print('\nРешение кодом с использованием генератора случайных чисел.')
from random import shuffle
src = list('7' * 40 + '9' * 40 + '4' * 50)
shuffle(src)
txt = ''.join(src)
print('Исходная случайная последовательность цифр в виде строки "%s" и длиной %d.\n' % (txt, len(txt)))
print('Применяем правила по условию задания, никаких дополнительных параметров не требуется.')
while ('49' in txt or '97' in txt or '47' in txt):
    if ('47' in txt):
        print('"47" найдено по индексу %d, ' % txt.find('47'), end='')
        txt = txt.replace('47', '74', 1)
        print('и после замены на "74" будет "%s".' % txt)
    if ('97' in txt):
        print('"97" найдено по индексу %d, ' % txt.find('97'), end='')
        txt = txt.replace('97', '79', 1)
        print('и после замены на "79" будет "%s".' % txt)
    if ('49' in txt):
        print('"49" найдено по индексу %d, ' % txt.find('49'), end='')
        txt = txt.replace('49', '94', 1)
        print('и после замены на "94" будет "%s".' % txt)
print('После схождения последовательности, на позициях %d:%s, %d:%s и %d:%s - ответ слитно.\n' %
      (24, txt[24], 70, txt[70], 104,txt[104]))
"""

"""
print('\n    Задание 55808, применим дополнительные данные для обработки правил через циклы. Этот вариант удобен '
      'тем, что параметр последовательности возрастает по мере движения по алгоритму. Вывод более компактный '
      'получается. Ответ: 16.')
rules = [['25', '3'], ['355', '52'], ['555', '23']]
print('\nРешение кодом с дополнительными правилами или парами преобразований:', rules, end='\n\n')
n = 12; sum = 0
while (sum != 27 and n < 100):
    txt = '3' + '5' * n 
    while (True):
        print('Строка последовательности при параметре %d: "%s".' % (n, txt))
        isIn = False; i = 0
        while (i < len(rules) and not isIn):
            if (rules[i][0] in txt):
                isIn = True
            i += 1
        if (not isIn):
            break
        i = 0
        while (i < len(rules)):
            if (rules[i][0] in txt):
                txt = txt.replace(rules[i][0], rules[i][1], 1)
            i += 1
    sum = 0
    for digit in txt:
        sum += int(digit)
    print('Сумма после обработки последовательности: %d.\n' % sum)
    n += 1
print('Результат последовательность "%s", сумма %d при параметре %d.\n' % (txt, sum, n - 1))
"""
